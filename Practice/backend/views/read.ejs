<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Document</title>
    <link rel="stylesheet" href="/stylesheets/style.css">
    <script src="https://cdn.jsdelivr.net/npm/@tailwindcss/browser@4"></script>
</head>

<body>
    <div class="w-full min-h-screen bg-zinc-900 text-white flex flex-col p-10 gap-10">
        <a href="/" class="cursor-pointer text-blue-500 text-lg">Back</a>
        <h1 class="text-4xl">All Users</h1>
        <div class="flex flex-wrap gap-5">
            <% allUsers.forEach(function(val){ %>
                <div class="w-72 p-5 bg-zinc-700 flex flex-col gap-2 justify-center items-center rounded-xl">
                    
                    <img class="w-60 h-60 bg-red-400 rounded" src= "<%= val.image %> ">
                    <h3 class="text-xl"><%= val.name %></h3>
                    <h5 class="text-zinc-500"> <%= val.email %> </h5>
                    <div class="flex justify-between w-full text-zinc-800">
                        <a class="p-1 bg-amber-200 rounded" href="/edit/<%= val._id %>">Edit</a>
                        <a class="bg-red-600 p-1 rounded" href="/delete/<%= val._id %>">Delete</a>
                    </div>
                </div>
                <% }) %>


        </div>
    </div>
</body>

</html>